# ProxOS

Just a fun side project.

## Features
* A custom (and very simple) operating system
* A custom Bytecode VM (.prx files)
* A simple shell for executing .prx files

## The VM
A simple, register based VM with a small heap.